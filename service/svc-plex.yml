kind: Service
apiVersion: v1
metadata:
  name: plex
  namespace: media
spec:
  type: LoadBalancer
  selector:
    app: plex
  ports:
    - name: pms
      port: 32400
      protocol: TCP
      targetPort: pms